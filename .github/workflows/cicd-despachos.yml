name: CI/CD Back Despachos
on:
  push:
    paths:
      - 'back-Despachos_SpringBoot/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./back-Despachos_SpringBoot/Springboot-API-REST-DESPACHO
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Run tests
        run: mvn test -Dspring.profiles.active=test

      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build and Push
        env:
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t ${{ secrets.ECR_REPO_BACK_DESPACHOS }}:$IMAGE_TAG -t ${{ secrets.ECR_REPO_BACK_DESPACHOS }}:latest .
          docker push ${{ secrets.ECR_REPO_BACK_DESPACHOS }}:$IMAGE_TAG
          docker push ${{ secrets.ECR_REPO_BACK_DESPACHOS }}:latest

      - name: Configurar kubectl
        working-directory: ${{ github.workspace }}
        run: aws eks update-kubeconfig --region ${{ secrets.AWS_REGION }} --name devopseks

      - name: Deploy a EKS
        working-directory: ${{ github.workspace }}
        env:
          IMAGE_TAG: ${{ github.sha }}
        run: |
          kubectl apply -f k8s/namespace.yaml
          sed -i "s|image: .*|image: ${{ secrets.ECR_REPO_BACK_DESPACHOS }}:$IMAGE_TAG|g" k8s/despachos.yaml
          kubectl apply -f k8s/despachos.yaml
          kubectl rollout status deployment/backend-despachos -n innovatech --timeout=120s