name: CI/CD Tienda Frontend

on:
  push:
    branches: [ "main" ]
    # Se activa solo si hay cambios dentro de la carpeta frontend
    paths:
      - 'frontend/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    # ESTO ES VITAL: Le dice a GitHub que todos los comandos 'run' 
    # se ejecuten dentro de la subcarpeta 'frontend'
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Configure Aws Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ secrets.AWS_REGION }}
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}

      - name: Login to ECR
        # ASIGNAR ESTE ID ES CRÍTICO para que el registro no llegue vacío
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build, tag, and push image to Amazon ECR
        env:
          # Referencia exacta al ID definido arriba
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: tienda-frontend
          IMAGE_TAG: ${{ github.sha }}
        run: |
          # Ahora $ECR_REGISTRY tendrá el valor real y el tag ya no empezará con "/"
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG \
                       -t $ECR_REGISTRY/$ECR_REPOSITORY:run-${{ github.run_number }} \
                       -t $ECR_REGISTRY/$ECR_REPOSITORY:latest .
          
          # Subimos todas las etiquetas profesionales a AWS ECR
          docker push --all-tags $ECR_REGISTRY/$ECR_REPOSITORY

      - name: Deploy to EC2 via SSM
        # Regresamos a la raíz del repo (.) para ejecutar el comando de AWS CLI
        working-directory: .
        run: |
          aws ssm send-command \
            --instance-ids ${{ secrets.EC2_FRONTEND_INSTANCE_ID }} \
            --document-name "AWS-RunShellScript" \
            --parameters 'commands=[
              "aws ecr get-login-password --region ${{ secrets.AWS_REGION }} | docker login --username AWS --password-stdin ${{ secrets.ECR_REGISTRY }}",
              "docker pull ${{ secrets.ECR_REPO_URL_FRONTEND }}:latest",
              "docker stop tienda-frontend || true",
              "docker rm tienda-frontend || true",
              "docker run -d --name tienda-frontend -p 80:80 ${{ secrets.ECR_REPO_URL_FRONTEND }}:latest"
            ]' \
            --region ${{ secrets.AWS_REGION }}